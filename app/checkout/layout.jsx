export const metadata = {
  title: "Checkout | AI Digital",
  description: "Secure checkout for your digital marketing services.",
  alternates: {
    canonical: "/checkout",
  }
};

export default function CheckoutLayout({ children }) {
  return children;
}
